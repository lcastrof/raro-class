export type VideoThumbnailProps = {
  id: string;
  nome: string;
  url: string;
  thumbUrl: string;
  descricao: string;
  createdAt: string;
  duracao: string;
  dataPublicacao: Date;
  topico: string;
  tags: string[];
};
